# Kev Agent Kit

@AGENTS.md

The project's `.mcp.json` exposes Kev through a local STDIO adapter. Use
`/kev-decision` for the shared interpretation guidance. Setup instructions for
Claude Code and Codex are in `docs/codex-local.md`.
