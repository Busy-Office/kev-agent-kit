[project]
name = "kev-mcp"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["mcp>=1.28,<2", "httpx>=0.28,<1", "pydantic>=2.9,<3", "tomlkit>=0.13,<1"]

[dependency-groups]
dev = ["pytest>=8,<10"]
