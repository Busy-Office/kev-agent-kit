{
  "mcpServers": {
    "kev": {
      "type": "stdio",
      "command": "sh",
      "args": [
        "-c",
        "exec uv run --frozen --directory \"$(git rev-parse --show-toplevel)/integrations/kev-mcp\" python kev_mcp.py"
      ],
      "env": {
        "KEV_BASE_URL": "http://127.0.0.1:8008",
        "KEV_TIMEOUT_SECONDS": "90"
      }
    }
  }
}
